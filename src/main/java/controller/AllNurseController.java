/*package controller;

import datastorage.DAOFactory;
import datastorage.PatientDAO;
import datastorage.TreatmentDAO;
import datastorage.NurseDAO;
import javafx.fxml.FXML;
import javafx.scene.control.*;
import javafx.stage.Stage;
import model.Patient;
import model.Treatment;
import utils.DateConverter;

import java.sql.SQLException;
import java.time.LocalDate;

public class AllNurseController {


       /* @FXML
        private Label lblPatientName;
        @FXML
        private Label lblCarelevel;
        @FXML
        private TextField txtBegin;
        @FXML
        private TextField txtEnd;
        @FXML
        private TextField txtDescription;
        @FXML
        private TextArea taRemarks;
        @FXML
        private DatePicker datepicker;
        @FXML
        private Button btnChange;
        @FXML
        private Button btnCancel;

        private AllNurseController controller;
        private Stage stage;
        private Patient patient;
        private Treatment treatment;
        private Nurse nurse;

        public void initializeController(AllNurseController controller, Stage stage, Nurse nurse) {
            this.stage = stage;
            this.controller= controller;
            //PatientDAO pDao = DAOFactory.getDAOFactory().createPatientDAO();
            try {

            } catch (SQLException e) {

            }
        }

        private void showData(){

        }

        @FXML
        public void handleChange(){

        }

        private void doUpdate(){
            NurseDAO dao = DAOFactory.getDAOFactory().createNurseDAO();
            try {
                dao.update(nurse);
            } catch (SQLException e) {
                e.printStackTrace();
            }
        }

        @FXML
        public void handleCancel(){
            stage.close();
        }

}*/
